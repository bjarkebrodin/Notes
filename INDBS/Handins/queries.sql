SELECT 'id --> pid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT id  FROM Projects  GROUP BY id  HAVING COUNT(DISTINCT pid) > 1  ) X;
SELECT 'id --> sid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT id  FROM Projects  GROUP BY id  HAVING COUNT(DISTINCT sid) > 1  ) X;
SELECT 'id --> sn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT id  FROM Projects  GROUP BY id  HAVING COUNT(DISTINCT sn) > 1  ) X;
SELECT 'id --> pn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT id  FROM Projects  GROUP BY id  HAVING COUNT(DISTINCT pn) > 1  ) X;
SELECT 'id --> mid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT id  FROM Projects  GROUP BY id  HAVING COUNT(DISTINCT mid) > 1  ) X;
SELECT 'id --> mn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT id  FROM Projects  GROUP BY id  HAVING COUNT(DISTINCT mn) > 1  ) X;
SELECT 'pid --> id' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pid  FROM Projects  GROUP BY pid  HAVING COUNT(DISTINCT id) > 1  ) X;
SELECT 'pid --> sid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pid  FROM Projects  GROUP BY pid  HAVING COUNT(DISTINCT sid) > 1  ) X;
SELECT 'pid --> sn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pid  FROM Projects  GROUP BY pid  HAVING COUNT(DISTINCT sn) > 1  ) X;
SELECT 'pid --> pn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pid  FROM Projects  GROUP BY pid  HAVING COUNT(DISTINCT pn) > 1  ) X;
SELECT 'pid --> mid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pid  FROM Projects  GROUP BY pid  HAVING COUNT(DISTINCT mid) > 1  ) X;
SELECT 'pid --> mn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pid  FROM Projects  GROUP BY pid  HAVING COUNT(DISTINCT mn) > 1  ) X;
SELECT 'sid --> id' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sid  FROM Projects  GROUP BY sid  HAVING COUNT(DISTINCT id) > 1  ) X;
SELECT 'sid --> pid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sid  FROM Projects  GROUP BY sid  HAVING COUNT(DISTINCT pid) > 1  ) X;
SELECT 'sid --> sn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sid  FROM Projects  GROUP BY sid  HAVING COUNT(DISTINCT sn) > 1  ) X;
SELECT 'sid --> pn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sid  FROM Projects  GROUP BY sid  HAVING COUNT(DISTINCT pn) > 1  ) X;
SELECT 'sid --> mid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sid  FROM Projects  GROUP BY sid  HAVING COUNT(DISTINCT mid) > 1  ) X;
SELECT 'sid --> mn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sid  FROM Projects  GROUP BY sid  HAVING COUNT(DISTINCT mn) > 1  ) X;
SELECT 'sn --> id' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sn  FROM Projects  GROUP BY sn  HAVING COUNT(DISTINCT id) > 1  ) X;
SELECT 'sn --> pid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sn  FROM Projects  GROUP BY sn  HAVING COUNT(DISTINCT pid) > 1  ) X;
SELECT 'sn --> sid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sn  FROM Projects  GROUP BY sn  HAVING COUNT(DISTINCT sid) > 1  ) X;
SELECT 'sn --> pn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sn  FROM Projects  GROUP BY sn  HAVING COUNT(DISTINCT pn) > 1  ) X;
SELECT 'sn --> mid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sn  FROM Projects  GROUP BY sn  HAVING COUNT(DISTINCT mid) > 1  ) X;
SELECT 'sn --> mn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT sn  FROM Projects  GROUP BY sn  HAVING COUNT(DISTINCT mn) > 1  ) X;
SELECT 'pn --> id' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pn  FROM Projects  GROUP BY pn  HAVING COUNT(DISTINCT id) > 1  ) X;
SELECT 'pn --> pid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pn  FROM Projects  GROUP BY pn  HAVING COUNT(DISTINCT pid) > 1  ) X;
SELECT 'pn --> sid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pn  FROM Projects  GROUP BY pn  HAVING COUNT(DISTINCT sid) > 1  ) X;
SELECT 'pn --> sn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pn  FROM Projects  GROUP BY pn  HAVING COUNT(DISTINCT sn) > 1  ) X;
SELECT 'pn --> mid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pn  FROM Projects  GROUP BY pn  HAVING COUNT(DISTINCT mid) > 1  ) X;
SELECT 'pn --> mn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT pn  FROM Projects  GROUP BY pn  HAVING COUNT(DISTINCT mn) > 1  ) X;
SELECT 'mid --> id' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mid  FROM Projects  GROUP BY mid  HAVING COUNT(DISTINCT id) > 1  ) X;
SELECT 'mid --> pid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mid  FROM Projects  GROUP BY mid  HAVING COUNT(DISTINCT pid) > 1  ) X;
SELECT 'mid --> sid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mid  FROM Projects  GROUP BY mid  HAVING COUNT(DISTINCT sid) > 1  ) X;
SELECT 'mid --> sn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mid  FROM Projects  GROUP BY mid  HAVING COUNT(DISTINCT sn) > 1  ) X;
SELECT 'mid --> pn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mid  FROM Projects  GROUP BY mid  HAVING COUNT(DISTINCT pn) > 1  ) X;
SELECT 'mid --> mn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mid  FROM Projects  GROUP BY mid  HAVING COUNT(DISTINCT mn) > 1  ) X;
SELECT 'mn --> id' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mn  FROM Projects  GROUP BY mn  HAVING COUNT(DISTINCT id) > 1  ) X;
SELECT 'mn --> pid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mn  FROM Projects  GROUP BY mn  HAVING COUNT(DISTINCT pid) > 1  ) X;
SELECT 'mn --> sid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mn  FROM Projects  GROUP BY mn  HAVING COUNT(DISTINCT sid) > 1  ) X;
SELECT 'mn --> sn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mn  FROM Projects  GROUP BY mn  HAVING COUNT(DISTINCT sn) > 1  ) X;
SELECT 'mn --> pn' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mn  FROM Projects  GROUP BY mn  HAVING COUNT(DISTINCT pn) > 1  ) X;
SELECT 'mn --> mid' AS FD,  CASE WHEN COUNT(*)=0 THEN 'MAY HOLD'  ELSE 'does not hold' END AS VALIDITY  FROM (  SELECT mn  FROM Projects  GROUP BY mn  HAVING COUNT(DISTINCT mid) > 1  ) X;
